name: CICD

on :
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
      steps:
        -name: clone the code
        run: echo:cloning the code"
build:
  runs-on: ubuntu-latest
      steps:
        -name: build using docker
        run: echo:building  the code"
test:
  runs-on: ubuntu-latest
      steps:
        -name: build using docker
        run: echo:building  the code"
deploy:
  runs-on: ubuntu-latest
      steps:
        -name: build using docker
        run: echo:building  the code"
